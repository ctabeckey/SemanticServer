{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "The schema for communicating the available processor implemenattions.",
  "definitions": {
      "property": {
        "type": "object",
        "properties": {
          "key": {"type": "string"},
          "type": {"type": "string"},
          "description" :{"type": "string"}
        }
      },
      "context-property": {
        "$ref": "#/definitions/property"
      },
      "processor-property": {
        "$ref": "#/definitions/property"
      },
      "processor": {
        "type": "object",
        "properties": {
            "processor": {"type": "string"},
            "configuration": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/processor-property"
              }
            }
        }
      }
  },
  "type": "object",
  "properties": {
    "processors": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/processor"
      }
    }
  }
}
