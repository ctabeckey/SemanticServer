/**
 * This package defines a CommandFactory that creates commands implementing a workflow.
 * A workflow can be defined as an external resource and loaded at runtime.
 * This package should be moved into its own module eventually.
 */
package com.paypal.credit.workflowcommand;